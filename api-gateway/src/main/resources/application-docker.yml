server:
  port: 9000
spring:
  cloud:
    gateway:
      routes:
        - id: appointment_id
          uri: 'http://appointment-api:8000'
          predicates:
            - Path=/health-service/**
  application:
    name: API-GATEWAY
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://service-discovery:9001/eureka/
    healthcheck: true
  instance:
    hostname: api-gateway